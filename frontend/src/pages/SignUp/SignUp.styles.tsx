import styled from 'styled-components';
import { SignInFooter, SignInForm, SignInHeader, SignInTitle, ToSignUpButton } from '../SignIn/SignIn.styles';

export const SignUpHeader = styled(SignInHeader)``;

export const SignUpTitle = styled(SignInTitle)``;

export const SignUpForm = styled(SignInForm)``;

export const SignUpFooter = styled(SignInFooter)``;

export const ToSignInButton = styled(ToSignUpButton)``;
